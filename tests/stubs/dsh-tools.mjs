// Checkout-less stub for @deepseek-ai/dsh-tools (its npm rc pulls a chain of
// unpublished peers). The plugin uses defineTool as an identity wrapper only.
export const defineTool = (definition) => definition

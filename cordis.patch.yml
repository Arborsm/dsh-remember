# Bundle layer: applied when a profile lists @arborsm/dsh-memory-plugin. Rows
# resolve by package name so Node module resolution finds the installed code.
- insert:
    - id: dsh-memory
      name: '@arborsm/dsh-memory-plugin'
